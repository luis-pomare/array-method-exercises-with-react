import StaticData from './static-data';
import DynamicData from './dynamic-data';

export default function Main() {
  return (
    <main>
      <StaticData />
      <DynamicData />
    </main>
  );
}
